# Pin the audio stack here so a stale system transformers cannot boot-fail.
# kupe 0.2.x has no subfolder=; 0.3 lives on git until it is on PyPI.
# tokenizers>=0.23 has no reliable wheels yet — pip compiles Rust and hangs.
kupe @ git+https://github.com/kupe-ai/kupe-sdk.git@main
transformers>=4.49,<4.58
tokenizers>=0.21,<0.22
accelerate>=0.34
huggingface_hub>=0.34,<1
hf_transfer
sounddevice>=0.4
websockets>=12.0
openai>=1.40
httpx>=0.27
rich>=13.0
numpy>=1.24
pyrnnoise>=0.1
fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0
pyngrok>=7.2
gradio>=4.44,<6
fastrtc>=0.0.19
