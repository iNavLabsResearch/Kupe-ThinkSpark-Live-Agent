# from git until 0.3.0 is on PyPI — 0.2.x lacks subfolder= and still fetches gated Gemma
kupe[thinkspark] @ git+https://github.com/kupe-ai/kupe-sdk.git@main
sounddevice>=0.4
websockets>=12.0
openai>=1.40
httpx>=0.27
rich>=13.0
numpy>=1.24
pyrnnoise>=0.1              # RNNoise denoising (optional; passthrough if absent)
fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0
